@using Microsoft.AspNetCore.Identity
@model IEnumerable<IdentityRole>
@{
	ViewData["Title"] = "Seznam rolí";
}

<h1 class="text-white text-center">Seznam rolí</h1>
@* <a asp-action="Create" class="btn btn-secondary">Vytvořit roli</a> *@
<div asp-validation-summary="All" class="custom-validation-summary"></div>
<div class="d-flex align-content-center justify-content-center">
	<table class="table table-success table-striped" style="width:10rem">

		@foreach (var role in Model)
		{
			<tr>
				<td>@role.Name</td>
				<td>
					<a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-id="@role.Id">Editovat</a>
				</td>
				@* <td>
			<form asp-action="Delete" asp-route-id="@role.Id"
			method="post">
			<button type="submit" class="btn btn-sm btn-danger">
			Delete
			</button>
			</form>
			</td> *@
				@* <td>@role.Id</td> *@
			</tr>
		}
	</table>
</div>